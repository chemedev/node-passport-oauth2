<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>OAuth Tutorials</title>
    <style>
      body {
        font-family: arial;
        margin: 0;
      }
      nav {
        background: #ff5353;
        padding: 20px 10px;
      }
      nav ul {
        position: relative;
        max-width: 960px;
        margin: 0 auto;
        padding: 0;
      }
      nav li {
        list-style-type: none;
        display: inline-block;
        margin: 0 10px 0 0;
      }
      nav li:nth-child(3) {
        position: absolute;
        right: 41px;
      }
      nav li.login {
        right: 0;
        margin-right: 0;
      }
      nav li:nth-child(4) {
        position: absolute;
        margin-right: 0;
        right: 0px;
      }
      nav a {
        color: #fff;
        font-size: 18px;
        background: rgba(255, 255, 255, 0.2);
        text-decoration: none;
        padding: 10px;
        display: block;
      }
      nav a:hover {
        outline: 5px solid rgba(255, 255, 255, 0.3);
      }
      main,
      header {
        max-width: 960px;
        margin: 30px auto;
        padding: 0 10px;
        color: #333;
      }
      nav img {
        width: 41px;
        height: 41px;
      }
    </style>
  </head>
  <body>
    <nav>
      <ul>
        <li><a href="/">Homepage</a></li>
        <li><a href="/profile">Profile</a></li>
        <% if (user) { %>
        <li><a href="/auth/logout">Logout</a></li>
        <li><img src="<%= user.picture %>" alt="user photograph" /></li>
        <% } else { %>
        <li class="login" ><a href="/auth/login">Login</a></liclass="login" >
        <% } %>
      </ul>
    </nav>
    <header>
      <h1>Net Ninja OAuth Tut's</h1>
      <h2>Homepage</h2>
    </header>
    <main>
      <p>Probably the best OAuth tutorials on the planet :)</p>
    </main>
  </body>
</html>
